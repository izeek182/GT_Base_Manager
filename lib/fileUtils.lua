if (_FileUtil == nil) then
    _FileUtil = {
    }
    fs = require("filesystem")
    function _FileUtil.append(file,...)
    end

    function _FileUtil.size(file)
    end

    function _FileUtil.clear(file)
        
    end

    function _FileUtil.reader(file)
        
    end

end